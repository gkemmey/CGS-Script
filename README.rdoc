= Diary to HTML

This is a script in Ruby that process a file full of diary-like entries, makes an HTML page for each with some simple styling, and links the most frequent rewards in the entry to other diary pages.